import Logo from './logo';
import heroImageBase64 from './images/hero-image';
import contactImageBase64 from './images/contact-image';
import featuresImageBase64 from './images/features-image';

// Since we don't have actual image files, we'll use CSS gradients for backgrounds
const heroBg = 'linear-gradient(135deg, #00334e 0%, #005580 100%)';

export { Logo, heroBg, heroImageBase64, contactImageBase64, featuresImageBase64 }; 